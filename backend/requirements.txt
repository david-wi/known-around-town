fastapi==0.115.0
uvicorn[standard]==0.30.6
motor==3.6.0
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.10
jinja2==3.1.4
markdown2==2.5.0
python-slugify==8.0.4
httpx==0.27.2
pytest==8.3.3
pytest-asyncio==0.24.0
mongomock-motor==0.0.34
stripe==11.4.1
# WHY: EmailStr in owner_leads.py requires pydantic's email validation
# extra, which in turn requires this package. Without it the app crashes
# at startup before handling any request (including /pricing).
email-validator==2.2.0
# WHY: respx is the httpx-specific mock library used in test_google_places.py
# to intercept Places API HTTP calls without making real network requests.
respx==0.22.0
# WHY: esprima is a pure-Python JavaScript parser used by
# test_template_js_syntax.py to syntax-check every inline <script> block in our
# Jinja templates. We use a Python parser rather than `node --check` because the
# "Smoke tests" CI job (.github/workflows/ci.yml) only sets up Python — it never
# installs Node — so depending on a Node binary would make the guard rely on
# undeclared tooling that a future runner image could remove, silently letting
# the very bug class this test exists to catch slip through again.
esprima==4.0.1
